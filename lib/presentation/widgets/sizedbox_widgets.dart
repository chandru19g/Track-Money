import 'package:flutter/material.dart';

class PrimaryHeightSizeddBox extends StatelessWidget {
  const PrimaryHeightSizeddBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 16.0,
    );
  }
}

class PrimaryWidthSizeddBox extends StatelessWidget {
  const PrimaryWidthSizeddBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 16.0,
    );
  }
}

class PrimarySizeddBox extends StatelessWidget {
  const PrimarySizeddBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 16.0,
      width: 16.0,
    );
  }
}

class LargeSizeddBox extends StatelessWidget {
  const LargeSizeddBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 24.0,
      width: 24.0,
    );
  }
}

class LargeHeightSizedBox extends StatelessWidget {
  const LargeHeightSizedBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 24.0,
    );
  }
}

class SmallHeightSizedBox extends StatelessWidget {
  const SmallHeightSizedBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 4.0,
    );
  }
}

class SmallWidthSizedBox extends StatelessWidget {
  const SmallWidthSizedBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 4.0,
    );
  }
}

class SmallSizedBox extends StatelessWidget {
  const SmallSizedBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 4.0,
      width: 4.0,
    );
  }
}
